int compute ( int a, int d) {    
    int b;
    int c = 5;    
    b = a;   
     while (c > 0) {      
         b = b + a * 4;}   
          return b;}
void main(void) 
    {  
        int x;  
        if (x == 3) {  
              print(x);  } 
              else {    
               int y = compute(x, 2 * x); 
               print(y);  }}
